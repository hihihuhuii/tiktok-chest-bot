services:
  - type: web
    name: tiktok-chest-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python main.py
    plan: free
    region: singapore
